<p id="notice"><%= notice %></p>

<h1>Postagens</h1>

<%= link_to '+ postagem', new_post_path, class:"pull-right btn btn-info" %>

<table class="table">
  <thead>
    <tr>
      <th>Título</th>
      <th>Autor</th>
      <th>Categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.user.try(:name) %></td>
        <td><%= post.category.try(:name) %></td>
        <td><%= link_to 'Exibir', post %></td>
        <td><%= link_to 'Editar', edit_post_path(post) %></td>
        <td><%= link_to 'Deletar', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
